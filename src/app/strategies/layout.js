export const metadata = {
  title: 'Strategies — SMC Journal',
  description: 'Manage and analyse your SMC strategies. Win rate, average RR, and expectancy per setup.',
};

export default function StrategiesLayout({ children }) {
  return children;
}
