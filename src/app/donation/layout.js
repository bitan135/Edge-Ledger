export const metadata = {
  title: 'Donation — SMC Journal',
  description: 'Support the vision of SMC Journal. Completely free forever for the community.',
};

export default function DonationLayout({ children }) {
  return children;
}
