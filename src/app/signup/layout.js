export const metadata = {
  title: 'Create Account — SMC Journal',
  description: 'Create your free SMC Journal account. Start journaling your Smart Money trades today.',
};

export default function SignupLayout({ children }) {
  return children;
}
